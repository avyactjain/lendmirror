/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
    Account,
    Context,
    Pda,
    PublicKey,
    RpcAccount,
    RpcGetAccountOptions,
    RpcGetAccountsOptions,
    assertAccountExists,
    deserializeAccount,
    gpaBuilder,
    publicKey as toPublicKey,
} from '@metaplex-foundation/umi'
import {
    Serializer,
    array,
    bytes,
    mapSerializer,
    publicKey as publicKeySerializer,
    struct,
    u64,
    u8,
} from '@metaplex-foundation/umi/serializers'

export type CcipRoute = Account<CcipRouteAccountData>

export type CcipRouteAccountData = {
    discriminator: Uint8Array
    router: PublicKey
    feeQuoter: PublicKey
    rmnRemote: PublicKey
    linkMint: PublicKey
    /** CCIP chain selector of the destination (Sepolia is 16015286601757825753). */
    destChainSelector: bigint
    /** Ethereum LendMirror address, 20 bytes. CCIP receiver. */
    receiver: Array<number>
    /** Gas the destination `ccipReceive` may use. */
    gasLimit: bigint
    bump: number
}

export type CcipRouteAccountDataArgs = {
    router: PublicKey
    feeQuoter: PublicKey
    rmnRemote: PublicKey
    linkMint: PublicKey
    /** CCIP chain selector of the destination (Sepolia is 16015286601757825753). */
    destChainSelector: number | bigint
    /** Ethereum LendMirror address, 20 bytes. CCIP receiver. */
    receiver: Array<number>
    /** Gas the destination `ccipReceive` may use. */
    gasLimit: number | bigint
    bump: number
}

export function getCcipRouteAccountDataSerializer(): Serializer<CcipRouteAccountDataArgs, CcipRouteAccountData> {
    return mapSerializer<CcipRouteAccountDataArgs, any, CcipRouteAccountData>(
        struct<CcipRouteAccountData>(
            [
                ['discriminator', bytes({ size: 8 })],
                ['router', publicKeySerializer()],
                ['feeQuoter', publicKeySerializer()],
                ['rmnRemote', publicKeySerializer()],
                ['linkMint', publicKeySerializer()],
                ['destChainSelector', u64()],
                ['receiver', array(u8(), { size: 20 })],
                ['gasLimit', u64()],
                ['bump', u8()],
            ],
            { description: 'CcipRouteAccountData' }
        ),
        (value) => ({ ...value, discriminator: new Uint8Array([61, 87, 7, 199, 227, 253, 152, 120]) })
    ) as Serializer<CcipRouteAccountDataArgs, CcipRouteAccountData>
}

export function deserializeCcipRoute(rawAccount: RpcAccount): CcipRoute {
    return deserializeAccount(rawAccount, getCcipRouteAccountDataSerializer())
}

export async function fetchCcipRoute(
    context: Pick<Context, 'rpc'>,
    publicKey: PublicKey | Pda,
    options?: RpcGetAccountOptions
): Promise<CcipRoute> {
    const maybeAccount = await context.rpc.getAccount(toPublicKey(publicKey, false), options)
    assertAccountExists(maybeAccount, 'CcipRoute')
    return deserializeCcipRoute(maybeAccount)
}

export async function safeFetchCcipRoute(
    context: Pick<Context, 'rpc'>,
    publicKey: PublicKey | Pda,
    options?: RpcGetAccountOptions
): Promise<CcipRoute | null> {
    const maybeAccount = await context.rpc.getAccount(toPublicKey(publicKey, false), options)
    return maybeAccount.exists ? deserializeCcipRoute(maybeAccount) : null
}

export async function fetchAllCcipRoute(
    context: Pick<Context, 'rpc'>,
    publicKeys: Array<PublicKey | Pda>,
    options?: RpcGetAccountsOptions
): Promise<CcipRoute[]> {
    const maybeAccounts = await context.rpc.getAccounts(
        publicKeys.map((key) => toPublicKey(key, false)),
        options
    )
    return maybeAccounts.map((maybeAccount) => {
        assertAccountExists(maybeAccount, 'CcipRoute')
        return deserializeCcipRoute(maybeAccount)
    })
}

export async function safeFetchAllCcipRoute(
    context: Pick<Context, 'rpc'>,
    publicKeys: Array<PublicKey | Pda>,
    options?: RpcGetAccountsOptions
): Promise<CcipRoute[]> {
    const maybeAccounts = await context.rpc.getAccounts(
        publicKeys.map((key) => toPublicKey(key, false)),
        options
    )
    return maybeAccounts
        .filter((maybeAccount) => maybeAccount.exists)
        .map((maybeAccount) => deserializeCcipRoute(maybeAccount as RpcAccount))
}

export function getCcipRouteGpaBuilder(context: Pick<Context, 'rpc' | 'programs'>) {
    const programId = context.programs.getPublicKey('lendmirror', '')
    return gpaBuilder(context, programId)
        .registerFields<{
            discriminator: Uint8Array
            router: PublicKey
            feeQuoter: PublicKey
            rmnRemote: PublicKey
            linkMint: PublicKey
            destChainSelector: number | bigint
            receiver: Array<number>
            gasLimit: number | bigint
            bump: number
        }>({
            discriminator: [0, bytes({ size: 8 })],
            router: [8, publicKeySerializer()],
            feeQuoter: [40, publicKeySerializer()],
            rmnRemote: [72, publicKeySerializer()],
            linkMint: [104, publicKeySerializer()],
            destChainSelector: [136, u64()],
            receiver: [144, array(u8(), { size: 20 })],
            gasLimit: [164, u64()],
            bump: [172, u8()],
        })
        .deserializeUsing<CcipRoute>((account) => deserializeCcipRoute(account))
        .whereField('discriminator', new Uint8Array([61, 87, 7, 199, 227, 253, 152, 120]))
}

export function getCcipRouteSize(): number {
    return 173
}
