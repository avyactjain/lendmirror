/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
    Account,
    Context,
    Pda,
    PublicKey,
    RpcAccount,
    RpcGetAccountOptions,
    RpcGetAccountsOptions,
    assertAccountExists,
    deserializeAccount,
    gpaBuilder,
    publicKey as toPublicKey,
} from '@metaplex-foundation/umi'
import { Serializer, bytes, mapSerializer, struct } from '@metaplex-foundation/umi/serializers'
import { PositionSnapshot, PositionSnapshotArgs, getPositionSnapshotSerializer } from '../types'

export type PositionSnapshotAccount = Account<PositionSnapshotAccountAccountData>

export type PositionSnapshotAccountAccountData = { discriminator: Uint8Array; snapshot: PositionSnapshot }

export type PositionSnapshotAccountAccountDataArgs = { snapshot: PositionSnapshotArgs }

export function getPositionSnapshotAccountAccountDataSerializer(): Serializer<
    PositionSnapshotAccountAccountDataArgs,
    PositionSnapshotAccountAccountData
> {
    return mapSerializer<PositionSnapshotAccountAccountDataArgs, any, PositionSnapshotAccountAccountData>(
        struct<PositionSnapshotAccountAccountData>(
            [
                ['discriminator', bytes({ size: 8 })],
                ['snapshot', getPositionSnapshotSerializer()],
            ],
            { description: 'PositionSnapshotAccountAccountData' }
        ),
        (value) => ({ ...value, discriminator: new Uint8Array([50, 23, 39, 253, 85, 2, 15, 196]) })
    ) as Serializer<PositionSnapshotAccountAccountDataArgs, PositionSnapshotAccountAccountData>
}

export function deserializePositionSnapshotAccount(rawAccount: RpcAccount): PositionSnapshotAccount {
    return deserializeAccount(rawAccount, getPositionSnapshotAccountAccountDataSerializer())
}

export async function fetchPositionSnapshotAccount(
    context: Pick<Context, 'rpc'>,
    publicKey: PublicKey | Pda,
    options?: RpcGetAccountOptions
): Promise<PositionSnapshotAccount> {
    const maybeAccount = await context.rpc.getAccount(toPublicKey(publicKey, false), options)
    assertAccountExists(maybeAccount, 'PositionSnapshotAccount')
    return deserializePositionSnapshotAccount(maybeAccount)
}

export async function safeFetchPositionSnapshotAccount(
    context: Pick<Context, 'rpc'>,
    publicKey: PublicKey | Pda,
    options?: RpcGetAccountOptions
): Promise<PositionSnapshotAccount | null> {
    const maybeAccount = await context.rpc.getAccount(toPublicKey(publicKey, false), options)
    return maybeAccount.exists ? deserializePositionSnapshotAccount(maybeAccount) : null
}

export async function fetchAllPositionSnapshotAccount(
    context: Pick<Context, 'rpc'>,
    publicKeys: Array<PublicKey | Pda>,
    options?: RpcGetAccountsOptions
): Promise<PositionSnapshotAccount[]> {
    const maybeAccounts = await context.rpc.getAccounts(
        publicKeys.map((key) => toPublicKey(key, false)),
        options
    )
    return maybeAccounts.map((maybeAccount) => {
        assertAccountExists(maybeAccount, 'PositionSnapshotAccount')
        return deserializePositionSnapshotAccount(maybeAccount)
    })
}

export async function safeFetchAllPositionSnapshotAccount(
    context: Pick<Context, 'rpc'>,
    publicKeys: Array<PublicKey | Pda>,
    options?: RpcGetAccountsOptions
): Promise<PositionSnapshotAccount[]> {
    const maybeAccounts = await context.rpc.getAccounts(
        publicKeys.map((key) => toPublicKey(key, false)),
        options
    )
    return maybeAccounts
        .filter((maybeAccount) => maybeAccount.exists)
        .map((maybeAccount) => deserializePositionSnapshotAccount(maybeAccount as RpcAccount))
}

export function getPositionSnapshotAccountGpaBuilder(context: Pick<Context, 'rpc' | 'programs'>) {
    const programId = context.programs.getPublicKey('lendmirror', '')
    return gpaBuilder(context, programId)
        .registerFields<{ discriminator: Uint8Array; snapshot: PositionSnapshotArgs }>({
            discriminator: [0, bytes({ size: 8 })],
            snapshot: [8, getPositionSnapshotSerializer()],
        })
        .deserializeUsing<PositionSnapshotAccount>((account) => deserializePositionSnapshotAccount(account))
        .whereField('discriminator', new Uint8Array([50, 23, 39, 253, 85, 2, 15, 196]))
}

export function getPositionSnapshotAccountSize(): number {
    return 233
}
