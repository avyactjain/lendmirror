/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import { Context, Pda, PublicKey, Signer, TransactionBuilder, transactionBuilder } from '@metaplex-foundation/umi'
import { Serializer, bytes, mapSerializer, struct } from '@metaplex-foundation/umi/serializers'
import { ResolvedAccount, ResolvedAccountsWithIndices, getAccountMetasAndSigners } from '../shared'

// Accounts.
export type GetPythPriceInstructionAccounts = {
    payer?: Signer
    /**
     * Pyth shard-0 push-feed account. Same `PriceUpdateV2` type as a pull update.
     * Address: PDA([shard_u16_le, feed_id], pythWSnswVUd12oZpeFP8e9CVaEqJg25g1Vtc2biRsT)
     * or the Account Address on https://docs.pyth.network/price-feeds/core/push-feeds/solana
     */

    priceFeed: PublicKey | Pda
    priceStore: PublicKey | Pda
    store: PublicKey | Pda
    systemProgram?: PublicKey | Pda
}

// Data.
export type GetPythPriceInstructionData = { discriminator: Uint8Array; feedId: Uint8Array }

export type GetPythPriceInstructionDataArgs = { feedId: Uint8Array }

export function getGetPythPriceInstructionDataSerializer(): Serializer<
    GetPythPriceInstructionDataArgs,
    GetPythPriceInstructionData
> {
    return mapSerializer<GetPythPriceInstructionDataArgs, any, GetPythPriceInstructionData>(
        struct<GetPythPriceInstructionData>(
            [
                ['discriminator', bytes({ size: 8 })],
                ['feedId', bytes({ size: 32 })],
            ],
            { description: 'GetPythPriceInstructionData' }
        ),
        (value) => ({ ...value, discriminator: new Uint8Array([183, 108, 128, 176, 75, 191, 37, 198]) })
    ) as Serializer<GetPythPriceInstructionDataArgs, GetPythPriceInstructionData>
}

// Args.
export type GetPythPriceInstructionArgs = GetPythPriceInstructionDataArgs

// Instruction.
export function getPythPrice(
    context: Pick<Context, 'payer' | 'programs'>,
    input: GetPythPriceInstructionAccounts & GetPythPriceInstructionArgs
): TransactionBuilder {
    // Program ID.
    const programId = context.programs.getPublicKey('lendmirror', '')

    // Accounts.
    const resolvedAccounts = {
        payer: { index: 0, isWritable: true as boolean, value: input.payer ?? null },
        priceFeed: { index: 1, isWritable: false as boolean, value: input.priceFeed ?? null },
        priceStore: { index: 2, isWritable: true as boolean, value: input.priceStore ?? null },
        store: { index: 3, isWritable: true as boolean, value: input.store ?? null },
        systemProgram: { index: 4, isWritable: false as boolean, value: input.systemProgram ?? null },
    } satisfies ResolvedAccountsWithIndices

    // Arguments.
    const resolvedArgs: GetPythPriceInstructionArgs = { ...input }

    // Default values.
    if (!resolvedAccounts.payer.value) {
        resolvedAccounts.payer.value = context.payer
    }
    if (!resolvedAccounts.systemProgram.value) {
        resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
            'splSystem',
            '11111111111111111111111111111111'
        )
        resolvedAccounts.systemProgram.isWritable = false
    }

    // Accounts in order.
    const orderedAccounts: ResolvedAccount[] = Object.values(resolvedAccounts).sort((a, b) => a.index - b.index)

    // Keys and Signers.
    const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, 'programId', programId)

    // Data.
    const data = getGetPythPriceInstructionDataSerializer().serialize(resolvedArgs as GetPythPriceInstructionDataArgs)

    // Bytes Created On Chain.
    const bytesCreatedOnChain = 0

    return transactionBuilder([{ instruction: { keys, programId, data }, signers, bytesCreatedOnChain }])
}
