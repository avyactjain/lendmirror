/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import { Context, Pda, PublicKey, Signer, TransactionBuilder, transactionBuilder } from '@metaplex-foundation/umi'
import { Serializer, bytes, mapSerializer, struct } from '@metaplex-foundation/umi/serializers'
import { ResolvedAccount, ResolvedAccountsWithIndices, getAccountMetasAndSigners } from '../shared'

// Accounts.
export type RequestBridgeOndemandInstructionAccounts = {
    authority?: Signer
    wrapper: PublicKey | Pda
    ondemand: PublicKey | Pda
}

// Data.
export type RequestBridgeOndemandInstructionData = { discriminator: Uint8Array }

export type RequestBridgeOndemandInstructionDataArgs = {}

export function getRequestBridgeOndemandInstructionDataSerializer(): Serializer<
    RequestBridgeOndemandInstructionDataArgs,
    RequestBridgeOndemandInstructionData
> {
    return mapSerializer<RequestBridgeOndemandInstructionDataArgs, any, RequestBridgeOndemandInstructionData>(
        struct<RequestBridgeOndemandInstructionData>([['discriminator', bytes({ size: 8 })]], {
            description: 'RequestBridgeOndemandInstructionData',
        }),
        (value) => ({ ...value, discriminator: new Uint8Array([48, 234, 61, 254, 194, 226, 59, 44]) })
    ) as Serializer<RequestBridgeOndemandInstructionDataArgs, RequestBridgeOndemandInstructionData>
}

// Instruction.
export function requestBridgeOndemand(
    context: Pick<Context, 'identity' | 'programs'>,
    input: RequestBridgeOndemandInstructionAccounts
): TransactionBuilder {
    // Program ID.
    const programId = context.programs.getPublicKey('lendmirror', '')

    // Accounts.
    const resolvedAccounts = {
        authority: { index: 0, isWritable: false as boolean, value: input.authority ?? null },
        wrapper: { index: 1, isWritable: true as boolean, value: input.wrapper ?? null },
        ondemand: { index: 2, isWritable: false as boolean, value: input.ondemand ?? null },
    } satisfies ResolvedAccountsWithIndices

    // Default values.
    if (!resolvedAccounts.authority.value) {
        resolvedAccounts.authority.value = context.identity
    }

    // Accounts in order.
    const orderedAccounts: ResolvedAccount[] = Object.values(resolvedAccounts).sort((a, b) => a.index - b.index)

    // Keys and Signers.
    const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, 'programId', programId)

    // Data.
    const data = getRequestBridgeOndemandInstructionDataSerializer().serialize({})

    // Bytes Created On Chain.
    const bytesCreatedOnChain = 0

    return transactionBuilder([{ instruction: { keys, programId, data }, signers, bytesCreatedOnChain }])
}
