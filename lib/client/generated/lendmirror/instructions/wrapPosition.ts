/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import { Context, Pda, PublicKey, Signer, TransactionBuilder, transactionBuilder } from '@metaplex-foundation/umi'
import { Serializer, bytes, mapSerializer, struct, u16, u32 } from '@metaplex-foundation/umi/serializers'
import { ResolvedAccount, ResolvedAccountsWithIndices, getAccountMetasAndSigners } from '../shared'

// Accounts.
export type WrapPositionInstructionAccounts = {
    authority?: Signer
    wrapper: PublicKey | Pda
    systemProgram?: PublicKey | Pda
}

// Data.
export type WrapPositionInstructionData = { discriminator: Uint8Array; vaultId: number; nftId: number }

export type WrapPositionInstructionDataArgs = { vaultId: number; nftId: number }

export function getWrapPositionInstructionDataSerializer(): Serializer<
    WrapPositionInstructionDataArgs,
    WrapPositionInstructionData
> {
    return mapSerializer<WrapPositionInstructionDataArgs, any, WrapPositionInstructionData>(
        struct<WrapPositionInstructionData>(
            [
                ['discriminator', bytes({ size: 8 })],
                ['vaultId', u16()],
                ['nftId', u32()],
            ],
            { description: 'WrapPositionInstructionData' }
        ),
        (value) => ({ ...value, discriminator: new Uint8Array([181, 96, 26, 183, 0, 85, 175, 127]) })
    ) as Serializer<WrapPositionInstructionDataArgs, WrapPositionInstructionData>
}

// Args.
export type WrapPositionInstructionArgs = WrapPositionInstructionDataArgs

// Instruction.
export function wrapPosition(
    context: Pick<Context, 'identity' | 'programs'>,
    input: WrapPositionInstructionAccounts & WrapPositionInstructionArgs
): TransactionBuilder {
    // Program ID.
    const programId = context.programs.getPublicKey('lendmirror', '')

    // Accounts.
    const resolvedAccounts = {
        authority: { index: 0, isWritable: true as boolean, value: input.authority ?? null },
        wrapper: { index: 1, isWritable: true as boolean, value: input.wrapper ?? null },
        systemProgram: { index: 2, isWritable: false as boolean, value: input.systemProgram ?? null },
    } satisfies ResolvedAccountsWithIndices

    // Arguments.
    const resolvedArgs: WrapPositionInstructionArgs = { ...input }

    // Default values.
    if (!resolvedAccounts.authority.value) {
        resolvedAccounts.authority.value = context.identity
    }
    if (!resolvedAccounts.systemProgram.value) {
        resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
            'splSystem',
            '11111111111111111111111111111111'
        )
        resolvedAccounts.systemProgram.isWritable = false
    }

    // Accounts in order.
    const orderedAccounts: ResolvedAccount[] = Object.values(resolvedAccounts).sort((a, b) => a.index - b.index)

    // Keys and Signers.
    const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, 'programId', programId)

    // Data.
    const data = getWrapPositionInstructionDataSerializer().serialize(resolvedArgs as WrapPositionInstructionDataArgs)

    // Bytes Created On Chain.
    const bytesCreatedOnChain = 0

    return transactionBuilder([{ instruction: { keys, programId, data }, signers, bytesCreatedOnChain }])
}
