/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import { Context, Pda, PublicKey, Signer, TransactionBuilder, transactionBuilder } from '@metaplex-foundation/umi'
import { Serializer, bytes, mapSerializer, struct } from '@metaplex-foundation/umi/serializers'
import { ResolvedAccount, ResolvedAccountsWithIndices, getAccountMetasAndSigners } from '../shared'

// Accounts.
export type AttachOndemandInstructionAccounts = {
    authority?: Signer
    wrapper: PublicKey | Pda
    ondemand: PublicKey | Pda
    systemProgram?: PublicKey | Pda
}

// Data.
export type AttachOndemandInstructionData = { discriminator: Uint8Array }

export type AttachOndemandInstructionDataArgs = {}

export function getAttachOndemandInstructionDataSerializer(): Serializer<
    AttachOndemandInstructionDataArgs,
    AttachOndemandInstructionData
> {
    return mapSerializer<AttachOndemandInstructionDataArgs, any, AttachOndemandInstructionData>(
        struct<AttachOndemandInstructionData>([['discriminator', bytes({ size: 8 })]], {
            description: 'AttachOndemandInstructionData',
        }),
        (value) => ({ ...value, discriminator: new Uint8Array([27, 149, 48, 3, 155, 18, 71, 152]) })
    ) as Serializer<AttachOndemandInstructionDataArgs, AttachOndemandInstructionData>
}

// Instruction.
export function attachOndemand(
    context: Pick<Context, 'identity' | 'programs'>,
    input: AttachOndemandInstructionAccounts
): TransactionBuilder {
    // Program ID.
    const programId = context.programs.getPublicKey('lendmirror', '')

    // Accounts.
    const resolvedAccounts = {
        authority: { index: 0, isWritable: true as boolean, value: input.authority ?? null },
        wrapper: { index: 1, isWritable: false as boolean, value: input.wrapper ?? null },
        ondemand: { index: 2, isWritable: true as boolean, value: input.ondemand ?? null },
        systemProgram: { index: 3, isWritable: false as boolean, value: input.systemProgram ?? null },
    } satisfies ResolvedAccountsWithIndices

    // Default values.
    if (!resolvedAccounts.authority.value) {
        resolvedAccounts.authority.value = context.identity
    }
    if (!resolvedAccounts.systemProgram.value) {
        resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
            'splSystem',
            '11111111111111111111111111111111'
        )
        resolvedAccounts.systemProgram.isWritable = false
    }

    // Accounts in order.
    const orderedAccounts: ResolvedAccount[] = Object.values(resolvedAccounts).sort((a, b) => a.index - b.index)

    // Keys and Signers.
    const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, 'programId', programId)

    // Data.
    const data = getAttachOndemandInstructionDataSerializer().serialize({})

    // Bytes Created On Chain.
    const bytesCreatedOnChain = 0

    return transactionBuilder([{ instruction: { keys, programId, data }, signers, bytesCreatedOnChain }])
}
