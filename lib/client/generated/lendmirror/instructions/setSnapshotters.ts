/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import { Context, Pda, PublicKey, Signer, TransactionBuilder, transactionBuilder } from '@metaplex-foundation/umi'
import { Serializer, bytes, mapSerializer, struct } from '@metaplex-foundation/umi/serializers'
import { ResolvedAccount, ResolvedAccountsWithIndices, getAccountMetasAndSigners } from '../shared'
import { SetAllowlistParams, SetAllowlistParamsArgs, getSetAllowlistParamsSerializer } from '../types'

// Accounts.
export type SetSnapshottersInstructionAccounts = {
    admin: Signer
    store: PublicKey | Pda
}

// Data.
export type SetSnapshottersInstructionData = { discriminator: Uint8Array; params: SetAllowlistParams }

export type SetSnapshottersInstructionDataArgs = { params: SetAllowlistParamsArgs }

export function getSetSnapshottersInstructionDataSerializer(): Serializer<
    SetSnapshottersInstructionDataArgs,
    SetSnapshottersInstructionData
> {
    return mapSerializer<SetSnapshottersInstructionDataArgs, any, SetSnapshottersInstructionData>(
        struct<SetSnapshottersInstructionData>(
            [
                ['discriminator', bytes({ size: 8 })],
                ['params', getSetAllowlistParamsSerializer()],
            ],
            { description: 'SetSnapshottersInstructionData' }
        ),
        (value) => ({ ...value, discriminator: new Uint8Array([216, 51, 140, 32, 10, 79, 42, 106]) })
    ) as Serializer<SetSnapshottersInstructionDataArgs, SetSnapshottersInstructionData>
}

// Args.
export type SetSnapshottersInstructionArgs = SetSnapshottersInstructionDataArgs

// Instruction.
export function setSnapshotters(
    context: Pick<Context, 'programs'>,
    input: SetSnapshottersInstructionAccounts & SetSnapshottersInstructionArgs
): TransactionBuilder {
    // Program ID.
    const programId = context.programs.getPublicKey('lendmirror', '')

    // Accounts.
    const resolvedAccounts = {
        admin: { index: 0, isWritable: true as boolean, value: input.admin ?? null },
        store: { index: 1, isWritable: true as boolean, value: input.store ?? null },
    } satisfies ResolvedAccountsWithIndices

    // Arguments.
    const resolvedArgs: SetSnapshottersInstructionArgs = { ...input }

    // Accounts in order.
    const orderedAccounts: ResolvedAccount[] = Object.values(resolvedAccounts).sort((a, b) => a.index - b.index)

    // Keys and Signers.
    const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, 'programId', programId)

    // Data.
    const data = getSetSnapshottersInstructionDataSerializer().serialize(
        resolvedArgs as SetSnapshottersInstructionDataArgs
    )

    // Bytes Created On Chain.
    const bytesCreatedOnChain = 0

    return transactionBuilder([{ instruction: { keys, programId, data }, signers, bytesCreatedOnChain }])
}
